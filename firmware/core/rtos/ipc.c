/**
 * @file ipc.c
 * @brief IPC module — implementation via cfe.h stubs (Phase 1).
 * Phase 2: real CFE_SB calls through cFS PSP.
 * @author CODEX — Luna Ice Mapper FSW Lead
 */
#include "ipc.h"
/* All logic lives in inline stubs in ipc.h / cfe.h for Phase 1. */
